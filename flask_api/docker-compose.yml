services:
  flask-api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
      - ./data:/app/data   # pour stocker data.db de manière persistante
    restart: always
